# Base URL of the MediCare FastAPI backend (no trailing slash).
# The backend's routes are already prefixed with /api, so just point this
# at the server root — e.g. http://localhost:8000 in local development.
VITE_API_BASE_URL=http://localhost:8000
